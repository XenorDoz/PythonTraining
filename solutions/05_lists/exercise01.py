numbers = [4, 9, 2, 7, 5]

# Write the output of each line:
#   - The first value
#   - The second to last value (using a negative index)
#   - Every value except the first and last one (using a positive index and a negative index)

print(numbers[0])
print(numbers[-2])
print(numbers[1:-1])