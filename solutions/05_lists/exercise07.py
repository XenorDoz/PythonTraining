students = [
    ("Alice", 15),
    ("Bob", 12),
    ("Charlie", 17)
]

# Create a dictionary named `grades`
# Each key is the studend's name
# Each value is their grade

grades = {
    "Alice": 15,
    "Bob": 12,
    "Charlie": 17
}

# Display the grade of `"Charlie"` from the dictionary

print(grades["Charlie"])

# Add a new student `"Diana"` with grade `14`

grades["Diana"] = 14